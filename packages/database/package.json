{
  "name": "database",
  "version": "1.0.0",
  "dependencies": {
    "@prisma/client": "latest"
  },
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "devDependencies": {
    "prisma": "latest",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
